Start Jenkins
$ sudo systemctl enable jenkins

Enable Jenkins
$ sudo systemctl enable jenkins

Enable the Jenkins port 8080 in the firewall if needed
$ sudo ufw allow 8080

Open in the browser
# http://localhost:8080/
